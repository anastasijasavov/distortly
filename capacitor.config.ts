import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'test.ionic.app',
  appName: 'test-ionic-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
